{
	"name": "{{ project_name }}",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "bun ./index.html",
		"build": "bun build ./index.html --outdir dist --minify",
		"start": "bunx serve dist"
	},
	"dependencies": {
	}
}
